name: discord_handler_dart_sequelize
description: A Discord bot in Dart using nyxx + drift
version: 1.0.0

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  nyxx: ^6.0.0
  drift: ^2.0.0
  sqlite3: ^2.0.0
  dotenv: ^4.0.0
  http: ^1.0.0

dev_dependencies:
  build_runner: ^2.4.0
  drift_dev: ^2.0.0
  lints: ^3.0.0
